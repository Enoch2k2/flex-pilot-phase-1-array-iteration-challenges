# Array Iteration Challenges

Follow the guidelines listed in the index.js, as comments, in order to complete each function. To run the code, to see your results, type in the terminal:

```
  node index.js
```